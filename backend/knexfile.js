const { Client } = require("knex")

module.exports ={
    client:'mysql2',
    connection: {
        host: 'localhost',
        user: 'root', 
        password: 'Mpkl@9948',  
        database: 'users',
    },
};